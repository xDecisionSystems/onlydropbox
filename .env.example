# Base Dropbox path where selective sync rules are applied.
# Use "/" for Dropbox root.
PREFIX_PATH=/

# Comma-separated list of folder names to sync within PREFIX_PATH.
# Spaces around commas are allowed.
# Example:
# SYNC_FOLDERS=Work, Photos, Personal/Taxes
SYNC_FOLDERS=
